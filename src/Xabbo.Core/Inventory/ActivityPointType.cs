namespace Xabbo.Core;

/// <summary>
/// Represents a type of activity point.
/// </summary>
public enum ActivityPointType
{
    Ducket = 0,
    Seashell = 1,
    Heart = 2,
    GiftPoint = 3,
    Shell = 4,
    Diamond = 5
}
